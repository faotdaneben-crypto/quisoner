{
  "name": "@rs-br/parent",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "cd packages/surveys-app && npm run dev",
    "build": "npm run build --workspace=surveys-app",
    "start": "npm run start --workspace=surveys-app"
  }
}
